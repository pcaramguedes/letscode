// Javascript - Sintaxe Básica

// Camada de Armazenamento

// var chave = valor; 
// let chave = valor; 
// const chave = valor; 
var nome = "Leonardo";
let idade = 38;
const magro = false;

// Camada de Instruções

// comando(param1, param2);
var nome = prompt("Digite seu nome:", "Seu nome vai aqui!");

// comando(parametro);
alert("Boa Noite " + nome + "!");

// contexto.comando(parametro);
document.write("Boa Noite " + nome + "!");

// comando();
// print();
